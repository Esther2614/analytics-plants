version: 2

sources:
  - name: load_web
    database: LOAD
    schema: WEB_SCHEMA
    tables:
      - name: sessions
        freshness:
          warn_after:
            count: 1
            period: day
          error_after:
            count: 5
            period: day
        loaded_at_field: _fivetran_synced
      - name: page_views
        freshness:
          warn_after:
            count: 1
            period: day
          error_after:
            count: 5
            period: day
        loaded_at_field: _fivetran_synced
      - name: item_views
        freshness:
          warn_after:
            count: 1
            period: day
          error_after:
            count: 5
            period: day
        loaded_at_field: _fivetran_synced
      - name: orders
        freshness:
          warn_after:
            count: 1
            period: day
          error_after:
            count: 5
            period: day
        loaded_at_field: _fivetran_synced

  - name: load_drive
    database: LOAD
    schema: GOOGLE_DRIVE
    tables:
      - name: expenses
        freshness:
          warn_after:
            count: 1
            period: day
          error_after:
            count: 5
            period: day
        loaded_at_field: _fivetran_synced
      - name: hr_joins
        freshness:
          warn_after:
            count: 1
            period: day
          error_after:
            count: 5
            period: day
        loaded_at_field: _fivetran_synced
      - name: hr_quits
        freshness:
          warn_after:
            count: 1
            period: day
          error_after:
            count: 5
            period: day
        loaded_at_field: _fivetran_synced
      - name: returns
        freshness:
          warn_after:
            count: 1
            period: day
          error_after:
            count: 5
            period: day
        loaded_at_field: _fivetran_synced